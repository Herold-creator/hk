<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Nouvelle actualité — Vaz Admin</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Publiez une nouvelle actualité depuis l’espace administrateur Vaz Management.">
  <meta name="keywords" content="nouvelle actualité, admin, publication, Vaz Management">
  <meta name="author" content="Vaz Management Mineral">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body.new-post-page {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background-color: #E4F2FA;
      color: #2E4159;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    header.header-container {
      background-color: #A8D8F0;
      text-align: center;
      padding: 20px;
    }

    header h1 {
      font-size: 2rem;
      margin-bottom: 10px;
      color: #003E73;
    }

    .slogan {
      font-size: 1.2rem;
      font-style: italic;
      color: #2E4159;
    }

    main.confirmation-section {
      flex: 1;
      max-width: 600px;
      margin: auto;
      padding: 40px 25px;
      background-color: rgba(255,255,255,0.9);
      border-radius: 8px;
      box-shadow: 0 0 12px rgba(0,0,0,0.05);
    }

    form.admin-form {
      display: grid;
      gap: 20px;
    }

    label {
      font-weight: 500;
      margin-bottom: 5px;
    }

    input, textarea {
      padding: 12px;
      border: 1px solid #DCE6ED;
      border-radius: 6px;
      font-size: 1rem;
      font-family: inherit;
      width: 100%;
    }

    button {
      background-color: #1f86c5;
      color: white;
      padding: 12px 24px;
      border: none;
      border-radius: 6px;
      font-weight: bold;
      font-size: 1rem;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background-color: #155b8a;
    }

    a {
      display: inline-block;
      margin-top: 20px;
      color: #003E73;
      text-decoration: underline;
      font-size: 0.95rem;
    }

    footer {
      background-color: #A8D8F0;
      text-align: center;
      padding: 15px;
      font-size: 0.9rem;
      color: #003E73;
    }

    @media screen and (max-width: 480px) {
      header h1 {
        font-size: 1.8rem;
      }

      main.confirmation-section {
        padding: 30px 15px;
      }
    }
  </style>
</head>
<body class="new-post-page">
  <header class="header-container">
    <h1>📰 Publier une actualité</h1>
    <p class="slogan">Ajoutez une nouvelle information visible par les visiteurs</p>
  </header>

  <main class="confirmation-section">
    <form method="POST" action="/admin/new-post" class="admin-form" aria-label="Formulaire de publication">
      <div>
        <label for="title">Titre</label>
        <input type="text" id="title" name="title" placeholder="Titre de l'article" required>
      </div>

      <div>
        <label for="date">Date</label>
        <input type="date" id="date" name="date" required>
      </div>

      <div>
        <label for="content">Contenu</label>
        <textarea id="content" name="content" rows="6" placeholder="Texte de l'article..." required></textarea>
      </div>

      <button type="submit">✅ Publier</button>
    </form>

    <a href="/admin/dashboard">← Retour au tableau de bord</a>
  </main>

  <footer>
    <p>&copy; 2025 Vaz Management — Zone Admin</p>
  </footer>
</body>
</html>
